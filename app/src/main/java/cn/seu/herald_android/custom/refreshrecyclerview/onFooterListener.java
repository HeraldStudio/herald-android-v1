package cn.seu.herald_android.custom.refreshrecyclerview;

public interface onFooterListener {
    /**
     *
     * @param footerPosition 为footer在列表中的位置
     */
    void footerListener(int footerPosition);
}
