package cn.seu.herald_android.mod_local.todo;

public class TodoCacheManager {
    /*public static final String CACHE_NAME = "herald_todo";

    private Context context;

    private CacheHelper cacheHelper;

    private JSONObject cache;

    public TodoCacheManager(Context context){
        this.context = context;
        cacheHelper = new CacheHelper(context);
        String cacheStr = cacheHelper.getCache(CACHE_NAME);
        if(cacheStr.equals(""))
    }

*/
}
